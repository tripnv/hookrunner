# hookrunner

A lightweight local server that listens for GitHub webhook events, watches for `/cc` mentions in PRs and issues, and dispatches local workflows in response — like triggering Claude for automated PR reviews on your machine.
